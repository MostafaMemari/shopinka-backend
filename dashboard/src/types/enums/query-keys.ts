export enum QueryKeys {
  Attributes = 'attributes',
  GalleryItems = 'gallery-items',
  Categories = 'categories',
  Products = 'products',
  Galleries = 'galleries',
  Comments = 'comments',
  Blogs = 'blogs'
}
